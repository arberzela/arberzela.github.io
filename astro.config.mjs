// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://arberzela.github.io',
  // Uncomment if your repo name is different from username.github.io
  // base: '/repo-name',
});
